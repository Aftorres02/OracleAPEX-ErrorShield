### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon

# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
AGENTS.md
release/release_log_*
release/&logname.lst
# _release.sql's spool lands here if it's ever run from the repo root
# instead of release/ (its documented cwd).
/release_log_*

# Private planning workspace (charter, roadmap, backlog) — internal, never
# meant to ship in the public repo.
/data/plan/
.claude
